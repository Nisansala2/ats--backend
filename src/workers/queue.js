// queue.js — Redis removed

function enqueueCVProcessing(cvId, s3Key, filename) {
  // Queue removed — do nothing
  console.log("Queue disabled. No background processing executed.");
}

module.exports = { enqueueCVProcessing };
